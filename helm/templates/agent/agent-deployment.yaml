apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agent
  template:
    metadata:
      labels:
        app: agent
    spec:
      initContainers:
        - name: wait-for-llm
          image: busybox
          command:
            - sh
            - -c
            - until wget -qO- http://llm:11434 >/dev/null 2>&1; do sleep 2; done
        - name: wait-for-mongo
          image: busybox
          command:
            - sh
            - -c
            - until nc -z mongo 27017; do sleep 2; done
      containers:
        - name: agent
          image: "{{ .Values.agent.image.repository }}:{{ .Values.agent.image.tag }}"
          imagePullPolicy: {{ .Values.agent.image.pullPolicy }}
          env:
            - name: MODEL
              value: {{ .Values.llm.env.MODEL | quote }}
            - name: LLM_URL
              value: {{ .Values.agent.env.LLM_URL | quote }}
            - name: MONGO_URL
              value: {{ .Values.agent.env.MONGO_URL | quote }}
            - name: QUERY
              value: {{ .Values.agent.env.QUERY | quote }}
